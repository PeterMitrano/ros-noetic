package:
  name: ros-noetic-cv-bridge
  version: 1.15.0
source:
  git_url: https://github.com/ros-gbp/vision_opencv-release.git
  git_rev: release/noetic/cv_bridge/1.15.0-1
  folder: ros-noetic-cv-bridge/src/work
  patches:
    - patch/ros-noetic-cv-bridge.patch

build:
  script:
    sel(win): bld_catkin.bat
    sel(unix): build_catkin.sh
  number: 0
about:
  home: https://www.ros.org/
  license: BSD-3-Clause
  summary: |
    Robot Operating System

extra:
  recipe-maintainers:
    - ros-forge

requirements:
  build:
    - "{{ compiler('cxx') }}"
    - "{{ compiler('c') }}"
    - ninja
    - sel(unix): make
    - cmake
  host:
    - boost
    - libopencv 4.2
    - numpy
    - py-opencv 4.2
    - python
    - ros-noetic-catkin
    - ros-noetic-rosconsole
    - ros-noetic-rostest
    - ros-noetic-sensor-msgs
  run:
    - boost
    - libopencv
    - py-opencv
    - python
    - ros-noetic-rosconsole
    - ros-noetic-sensor-msgs
