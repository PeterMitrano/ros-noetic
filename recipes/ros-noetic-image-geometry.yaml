package:
  name: ros-noetic-image-geometry
  version: 1.15.0
source:
  git_url: https://github.com/ros-gbp/vision_opencv-release.git
  git_rev: release/noetic/image_geometry/1.15.0-1
  folder: ros-noetic-image-geometry/src/work

build:
  script:
    sel(win): bld_catkin.bat
    sel(unix): build_catkin.sh
  number: 0
about:
  home: https://www.ros.org/
  license: BSD-3-Clause
  summary: |
    Robot Operating System

extra:
  recipe-maintainers:
    - ros-forge

requirements:
  build:
    - "{{ compiler('cxx') }}"
    - "{{ compiler('c') }}"
    - ninja
    - sel(unix): make
    - cmake
  host:
    - libopencv 4.2
    - py-opencv 4.2
    - python
    - ros-noetic-catkin
    - ros-noetic-sensor-msgs
  run:
    - libopencv
    - py-opencv
    - python
    - ros-noetic-sensor-msgs
